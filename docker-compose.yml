version: "3.8"

services:
  recap:
    build: .
    env_file:
      - .env
    ports:
      - "5002:5002"
    restart: unless-stopped
    environment:
      - JOB_DB_PATH=/data/jobs.db
    volumes:
      - jobs-data:/data

volumes:
  jobs-data:
